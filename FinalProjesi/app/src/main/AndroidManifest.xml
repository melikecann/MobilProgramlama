<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlagQuizApp"
        tools:targetApi="31">

        <!-- Ana ekran activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Diğer activity'ler -->
        <activity android:name=".MapQuizSelectionActivity" android:exported="false" />
        <activity android:name=".GeneralKnowledgeActivity" android:exported="false" />
        <activity android:name=".CapitalQuizSelectionActivity" android:exported="false" />
        <activity android:name=".FlagQuizSelectionActivity" android:exported="false" />
        <activity android:name=".QuizActivity" android:exported="false" />

    </application>

</manifest>
